name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    # Shared pipeline — Comradery64/.github, pinned by commit SHA.
    uses: Comradery64/.github/.github/workflows/npm-ci.yml@d1dbf206cf411027bf8b11e587bd1499f7a9d6fb # v1
