name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    # Shared pipeline — Comradery64/.github, pinned by commit SHA.
    uses: Comradery64/.github/.github/workflows/npm-ci.yml@4d801babc41ba3f715523b1f43e23cde4ed0a7dc # v2 (isolation test)
