name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    # Shared pipeline — see Comradery64/.github. Pin to a SHA/tag of that repo
    # once it's pushed (replace @main below).
    uses: Comradery64/.github/.github/workflows/npm-ci.yml@main
