name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    # Shared pipeline — Comradery64/.github, pinned by commit SHA.
    uses: Comradery64/.github/.github/workflows/npm-ci.yml@1161ad4b72983d043db6adb53ef40c27c346691d # v3
